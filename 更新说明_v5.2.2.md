# 终学意语 v5.2.2

基于 v5.2.1 完整项目继续开发。

- 错句本新增“记住了 · 遮住答案重写”：查看答案时停用输入和检查，遮住后清空输入，凭记忆重新作答。
- 同一卡片内反复查看答案，只记录一次查看失败，避免累计多次不会。
- 修复做对一条后下一条“先跳过”按钮不显示的问题。
- 做对后锁定输入，切换下一条时清除输入错误提示并恢复按钮状态。
- 新按钮和提示提供英文翻译，保留已有中英切换功能。
- 保留包名、数据库结构、音频及原有固定签名配置。默认 versionCode 从85升为86，保留 Codemagic 注入版本号机制。

## 验证

项目原有45项 Python 构建前检查通过；新增测试编译并执行真实 Java 复习状态类，覆盖查看、遮住、重复查看、完成、跳过和下一条重置。

尚未执行 Android Gradle 编译、签名 APK 构建或真机 UI 验证。本包为完整源码项目，不是 APK。继续使用原 Codemagic 工作流和原固定签名生成安装包；本次没有提交或推送 GitHub。
