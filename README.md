# 终学意语 v5.4.1 编译修复

修复 CourseDistributionPlanner.java 第69行 lambda 捕获循环变量 lesson 引起的 compileReleaseJavaWithJavac 失败。排序改为引用本轮 final currentLesson，不改变课程分配规则。

验证：先使用真正的 Java 编译器复现用户给出的相同错误；修复后编译 CourseUnit 和 CourseDistributionPlanner 成功，并执行确定性排序、复习间隔、预算和挑战去重检查。新增 tools/course_planner_compile_check.py 已接入 Codemagic。49项项目检查全部通过。

本地没有执行完整 Android Release 构建，需在 Codemagic 重新构建确认 APK 结果。包名和原固定签名配置保留，默认 versionCode 为89。将此完整项目解压到原仓库根目录，替换同名源码后提交；继续运行原 Codemagic 工作流。本次没有推送 GitHub。

## 历史版本说明

# 终学意语 v5.4.0

基于 v5.3.0 完整项目继续优化。

## 新增与修复

- 练习页新增最近使用的3个入口，重启后保留，重复使用会移到首位；仍遵循原有解锁规则。此记录是本机快捷入口，不计入学习成绩或进度备份。
- 增加一键清除搜索与筛选；筛选时隐藏最近使用区，专注显示匹配结果。
- 做题选项改为可增高布局，字号提高，选中项有浅蓝底色与边框提示。
- 显示当前题号；切换题目回到顶部，判分后滚动到反馈。
- 输入题增加 à è é ì ò ù ’ 快捷键，支持在光标位置插入或替换选中文字。
- 增加“不会，查看答案”，按未掌握记录，不计为答对；讲解页不显示该按钮。
- 修复已判分题目退出再进入时可能重复计分的问题：从下一题续学。
- 保留课程、音频、中英切换、数据库和固定签名设置。默认 versionCode 为88。

## 验证与构建

Codemagic 中48项 Python 检查全部通过。新增18项实际 Java 行为检查覆盖最近使用排序/去重/容量及不会判分、续学位置；原有检查继续通过。

未执行 Android Gradle 编译、签名 APK 构建、模拟器或真机 UI 验证。以上是源码与逻辑检查，不代表 APK 已打包。本包为完整项目源码，请使用原 Codemagic 工作流及原 zhongxue_release 签名身份构建。未推送 GitHub。

## 上一版说明

# 终学意语 v5.3.0

在 v5.2.2 完整项目上继续开发的 Android 原生意大利语学习 APP。

## 本版变化

- 首页优先展示今日任务与下一步操作，当前课程信息放在其后。
- 首页提供“背单词”和“查看复习日历”快捷入口。
- 练习页分为听力与开口、词汇与复习、语法练习、生活实战。
- 新增分类、离线中英文关键词搜索及“只看已解锁”。输入多个词时需全部匹配；英语不区分大小写，意大利语重音符号不影响匹配。
- 搜索保留原解锁规则。进阶工具仍遵循新手阶段和课程进度限制。
- 屏幕旋转时恢复搜索词、分类、已解锁筛选、进阶展开状态及首页详情展开状态。
- 按钮改为内容自适应高度，调整字号、间距和卡片层级；英文长按钮可换行。
- 首页折叠状态下不计算词汇/文章详细统计、不创建课程路线节点，展开后再生成；移除首次进入的重复刷新。
- 界面翻译只翻译输入提示，不改写用户正在输入的搜索词或练习答案。

保留 v5.2.2 的错句遮答案重写流程、课程内容、音频、中英切换、学习记录和固定签名配置。没有数据库结构变更。

## 构建与升级

这是源码项目，不是 APK。解压后 `app/`、`build.gradle`、`settings.gradle`、`codemagic.yaml` 位于项目根目录。使用原 Codemagic 工作流构建；新增筛选行为测试已接入构建前检查。

继续使用原 `zhongxue_release` 签名身份。包名保持 `com.italiano2774.nativeapp`，默认 versionCode 为 87；保留 Codemagic 注入版本号的机制。能否覆盖安装仍取决于实际构建使用相同签名且版本号高于已安装版本。

## 本地验证范围

- Codemagic 配置中47项 Python 检查通过，包括 Java 字面量、资源引用、XML、课程数据、构建脚本和签名配置等。
- 新增23项真实 Java 行为断言通过，覆盖分类、关键词、中英交叉搜索、重音、无匹配、锁定筛选及新界面翻译。
- v5.2.2 的12项错句复习状态行为检查继续通过。
- 检查首页与练习页按钮使用可增高布局以及不存在重复视图 ID。

本环境未执行 Android Gradle 编译、APK 签名构建、模拟器或真机界面验证；以上检查不等同于成功生成 APK。尚未测量真机加载耗时。未提交或推送 GitHub。
